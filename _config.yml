title: My Blog
description: A blog hosted on GitHub Pages
baseurl: "" # subpath, e.g. /blog (leave empty if at root)
url: "" # will be set by GitHub Pages

# Build settings
markdown: kramdown
permalink: /:year/:month/:day/:title/

# Plugins (GitHub Pages supported)
plugins:
  - jekyll-feed
